<form class="memo-list" action="" method="get">
    <% @memos.each do |memo| %>
      <a class='memo-list' href="/memos/<%= memo['id'].to_s %>">・<%= memo['title'] %></a>
      <br>
    <% end %>
</form>
</div>
<form class="memo-new" action="/memos/new" method="get">
  <button class="add-button">追加</button>
</form>
